# Flask serves the Vite development server separately by default.
BUILD=development

# Optional during the foundation migration. Persistence endpoints need MongoDB.
# MONGODB_URL=mongodb://localhost:27017

# Uploaded RDF files are stored locally and are ignored by Git.
UPLOAD_FOLDER=imports
